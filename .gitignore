# dependencies
/node_modules

# build output
/dist
*.tsbuildinfo

# env files (never commit secrets)
.env
.env.local
.env.*.local

# testing / playwright
/coverage
/test-results
/playwright-report

# supabase local
/supabase/.branches
/supabase/.temp

# scraper debug/capture output - never commit scraped payloads
src/scrapers/bookmakers/captured-json
/data/

# audit skill scratch state (reports live outside .tmp/)
docs/*-audit/.tmp/
docs/match-audit/.tmp/

# misc
.DS_Store
npm-debug.log*
*.pem
.playwright-mcp/
